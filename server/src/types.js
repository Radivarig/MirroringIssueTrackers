export type User = {
  username: string;
  id: number;
}

export type Repository = {
  name: string;
  id: number;
}

export type Issue = {
  id: number;
  title: string;
  body: string;

//  state: string;
//  asignees: Array;
//  labels: Array;
//  projects: Array;
//  milestones: Array;
}
